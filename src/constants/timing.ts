/**
 * Timing constants for consistent animation and interaction delays
 */

/** Duration for showing "copied" feedback after clipboard operations */
export const COPY_FEEDBACK_DURATION = 2000;

/** Duration for toast exit animation */
export const TOAST_EXIT_ANIMATION = 300;

/** Small delay for focus operations after DOM updates */
export const FOCUS_DELAY = 100;

/** Auto-play interval for carousels */
export const CAROUSEL_AUTO_PLAY_INTERVAL = 5000;

/** Delay before restarting carousel auto-play after user interaction */
export const CAROUSEL_RESUME_DELAY = 10000;

/** Simulated delay for mock AI responses */
export const MOCK_AI_DELAY = 800;

/** Delay for onboarding tooltip appearance */
export const ONBOARDING_DELAY = 500;

/** Debounce delay for file system operations */
export const REFRESH_DELAY = 100;

/** Delay for processing operations (file parsing, etc.) */
export const PROCESS_DELAY = 300;
